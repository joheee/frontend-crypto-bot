import MetalicButton from "../MetalicButton";

export default function PricingCard() {
  return (
    <div className="p-4 bg-secondary-black w-fit rounded-2xl">
      <MetalicButton text="Join Now" onClick={() => {}} />
    </div>
  );
}
